<Application x:Class="WindowsDashboard.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WindowsDashboard"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="TextBrush" Color="#F6F7FA"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#B7BECE"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#949DAD"/>
        <SolidColorBrush x:Key="CardBrush" Color="#232532"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#7A8294"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#9DB8E6"/>
        <SolidColorBrush x:Key="GreenBrush" Color="#7FC39A"/>
        <SolidColorBrush x:Key="YellowBrush" Color="#D5BD7A"/>
        <SolidColorBrush x:Key="RedBrush" Color="#D4877F"/>
        <SolidColorBrush x:Key="PurpleBrush" Color="#A18FD1"/>

        <Style x:Key="AiToolButton" TargetType="Button">
            <Setter Property="Background" Value="#07FFFFFF"/>
            <Setter Property="BorderBrush" Value="#12FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="AiBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="AiBorder" Property="Background" Value="#14FFFFFF"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="AiBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
